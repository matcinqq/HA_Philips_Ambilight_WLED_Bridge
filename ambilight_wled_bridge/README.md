# Ambilight WLED Bridge

Home Assistant app/add-on wrapper for the Philips Titan OS Ambilight to WLED bridge.

This app runs the same Python bridge used by the laptop CLI. JSON is still used for WLED preset/setup/restore, while live frames default to DDP over UDP.

See `DOCS.md` for setup and option notes.
